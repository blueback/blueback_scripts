version: '1'
services:
  virtual_raylib_run:
    image: virtual_raylib
    build:
      context: .
      no_cache: false
    container_name: virtual_raylib_run
    ports:
      - "5902:5902"
    volumes:
      - ${PWD}:${PWD}
    attach: false
    tty: true
    working_dir: ${PWD}
